import 'package:flutter/material.dart';

class TaxiInspectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Taxi Inspection')),
      body: Center(child: Text('Welcome to the Taxi Inspection Screen!')),
    );
  }
}
